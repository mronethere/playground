(function (define) {

    'use strict';

    define([],
        function () {
            var GameController = function () {

            };
            return [GameController];
        }
    );
})(define);